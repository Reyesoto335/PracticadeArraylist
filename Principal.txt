package ListadeAlumnos;

import Paquete_Datos_Lista_Alumnos.Persona;
import java.util.ArrayList;
//import java.util.Collection;
//import java.util.Collections;
//import java.util.Iterator;
import java.util.List;
import java.util.Scanner;

public class Principal {
    
    public static List<Persona> lista = new ArrayList();
    //public static Iterator<Persona> iterador = lista.iterator();
    static byte seleccion;
    static Scanner sc = new Scanner(System.in);
    public static void main(String[] args) {
        eleccion();
        
    }
    
    public static void eleccion (){
        System.out.println("\t\tSeleccione que quiere hacer \n\nIngresar nuevo alumno (1)\tDar listado de alumnos (2)\t"
                + "Salir y mostrar los alumnos agregados (3)");
        seleccion = sc.nextByte();
        switch (seleccion){
            case 1: ingresar();
                break;
            case 2: listadoalumnos();
                break;
            case 3: fin();
                break;
            default:
                break;
        }
    }
    
    public static void ingresar(){
        Persona alumnos = new Persona();
        //for(int i=0;i<1;i++){
        
        /*Iterator<Persona> listalumnos = lista.iterator();
        listalumnos = lista.iterator();
        while(listalumnos.hasNext()){
            for(int i=0;i<lista.size();i++){
        Persona elemento = listalumnos.next();
        
            if (elemento.getId() == elemento.getId()) {
                listalumnos.remove();
            }
        }
        }*/
        
        System.out.println("Ingrese el nombre del alumno: ");
        alumnos.setNombre(sc.next());
        System.out.println("Ingrese el apellido del alumno: ");
        alumnos.setApellido(sc.next());
        System.out.println("Ingrese la dirección del alumno: ");
        alumnos.setDirección(sc.next());
        System.out.println("Ingrese el ID del alumno: ");
        alumnos.setId(sc.nextInt());
        
        lista.add(alumnos);
        
        
        //}
        eleccion();
        
        /*Iterator<Persona> iterador = lista.iterator();
        while(iterador.hasNext()){
            System.out.println(iterador.next().getNombre());
            System.out.println(iterador.next().getApellido());
        }*/
        
    }
    
    public static void listadoalumnos(){
        for (Persona persona : lista) {
            System.out.println("NOMBRE\t\t\tAPELLIDO\t\t\t\tID");
            System.out.print(persona.getNombre()+"\t\t\t");
            System.out.print(persona.getApellido()+"\t\t\t\t\t");
            System.out.println(persona.getId()+"\n");
            
        }
        
        /*Iterator<Persona> iterador = lista.iterator();
        while(iterador.hasNext()){
            System.out.println(iterador.next().getNombre());
            System.out.println(iterador.next().getApellido());
        
        }*/
    
          eleccion();  
}
    public static void fin (){
        for (Persona persona : lista) {
            System.out.println("NOMBRE\t\t\tAPELLIDO\t\t\t\tID");
            System.out.print(persona.getNombre()+"\t\t\t");
            System.out.print(persona.getApellido()+"\t\t\t\t\t");
            System.out.println(persona.getId()+"\n");
        }
    }
}